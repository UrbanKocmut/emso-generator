Demo: https://delavnica.kocmut.com/

## Local preview

Run `serve.cmd` and open `http://127.0.0.1:8765/`. This uses a zero-dependency static server with the same browser loading model as GitHub Pages.

To use another port, run `serve.cmd 8766`. Stop the server with `Ctrl+C`.
